#!/bin/bash

# 現在のブランチを取得
current_branch=$(git rev-parse --abbrev-ref HEAD)

# test ブランチが含まれているか確認
if git branch --contains $(git rev-parse --short @) | grep -q 'test'; then
    echo "test ブランチが含まれているため、コミットは拒否されます。" >&2
    exit 1
fi

exit 0